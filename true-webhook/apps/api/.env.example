# Postgres connection string (recommended)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"

# CORS allow list (comma-separated) or *
# Example: CORS_ORIGIN="https://app.twm-monitors.com"
CORS_ORIGIN="*"
