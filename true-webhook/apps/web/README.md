# twm-monitor-web

Next.js frontend.

## Database
This app does not connect to the database directly.
It calls the API service, which is responsible for DB access.

## Env
- `NEXT_PUBLIC_API_BASE_URL` (example: `https://api.twm-monitors.com`)
